{
  "name": "test-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "test-package": "workspace:*"
  },
  "scripts": {
    "build": "echo \"[test-app] build stub — no-op\" && exit 0",
    "test": "echo \"[test-app] test stub — no-op\" && exit 0",
    "start": "tsx src/index.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
