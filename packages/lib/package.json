{
  "name": "test-package",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"[test-package] build stub — no-op\" && exit 0",
    "test": "echo \"[test-package] test stub — no-op\" && exit 0"
  }
}
